# This is a sample command please replace with correct command
spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/drunken_drive
    username: root
    #password: StrongPassword123!
    password: root

    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      schema: drunken_drive


  jpa:
    hibernate:
      ddl-auto: update
eurek:
  client:
    enabled: false

jwt:
  token:
    validity: 18000
    prefix: Bearer
  signing:
    key: signingkey
  authorities:
    key: roles
  header:
    string: Authorization

server:
  # port: 501
  port: 8085

intermediary:
  id: "1"
  code: "BC"